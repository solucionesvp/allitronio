import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Política de reembolso — Domina Google",
  alternates: { canonical: "/productos/domina-google/politica-de-reembolso" },
};

export default function PoliticaReembolsoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
